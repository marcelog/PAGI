NOTE
----
PAGI's autoloader has been renamed to the namespace PAGI\Autoloader and the
class name is now \PAGI\Autoloader\Autoloader so we now honor namespace to
filesystem convention for classes. Thus, change your code to:

require_once 'PAGI/Autoloader/Autoloader.php'; // Include pagi autoloader.
\PAGI\Autoloader\Autoloader::register(); // Call autoloader register for pagi autoloader.
